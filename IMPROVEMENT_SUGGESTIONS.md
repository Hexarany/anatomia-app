# Предложения по улучшению и добавлению контента
# Propuneri de îmbunătățire și adăugare de conținut

**Дата:** 20 декабря 2024
**Версия платформы:** 2.0+

---

## 📊 Анализ текущего состояния

### ✅ Что уже реализовано:
- Групповые чаты (веб + Telegram интеграция)
- Расписание занятий с автогенерацией
- YouTube видео в темах
- Отправка файлов группам через Telegram
- 3D модели анатомии
- Триггерные точки
- Протоколы массажа
- Квизы и тесты
- Библиотека ресурсов
- Система подписок (Free/Basic/Premium)
- Сертификаты и достижения
- Отслеживание прогресса
- Закладки и заметки
- Telegram Mini App
- Уведомления

---

## 🎯 ПРИОРИТЕТНЫЕ УЛУЧШЕНИЯ

### 1. 📝 Система домашних заданий и проверки работ

**Текущая ситуация:**
- Есть поле "домашнее задание" в расписании
- НЕТ системы сдачи и проверки работ

**Предлагаемая функциональность:**

#### Для студентов:
- Просмотр заданий по каждому занятию
- Загрузка выполненных работ:
  - Текстовые ответы
  - Файлы (PDF, DOC, изображения)
  - Фото выполненных упражнений
- Статус задания: "Не начато" / "В работе" / "Сдано" / "Проверено"
- Просмотр оценок и комментариев преподавателя
- История всех сданных работ
- Уведомления о дедлайнах

#### Для преподавателей:
- Создание заданий с дедлайнами
- Просмотр сданных работ студентов
- Выставление оценок (числовая или буквенная система)
- Комментарии к работам
- Массовая проверка работ группы
- Статистика по сданным/несданным работам
- Повторная отправка на доработку

#### Telegram интеграция:
- Команда `/homework` - список активных заданий
- Команда `/submit` - отправка работы через бота
- Уведомления:
  - "Новое домашнее задание по теме X"
  - "До дедлайна осталось 24 часа"
  - "Преподаватель проверил вашу работу"
  - "Оценка: 9/10. Комментарий: ..."

#### База данных:
```typescript
interface Assignment {
  _id: ObjectId;
  scheduleId: ObjectId;        // Связь с занятием
  title: { ru: string; ro: string };
  description: { ru: string; ro: string };
  deadline: Date;
  maxScore: number;            // Максимальный балл
  allowLateSubmission: boolean;
  files: string[];             // Прикрепленные файлы от преподавателя
  createdAt: Date;
}

interface Submission {
  _id: ObjectId;
  assignmentId: ObjectId;
  studentId: ObjectId;
  submittedAt: Date;
  textAnswer?: string;
  files: string[];             // Загруженные студентом файлы
  status: 'submitted' | 'graded' | 'revision';
  grade?: number;
  feedback?: string;           // Комментарий преподавателя
  gradedAt?: Date;
  gradedBy?: ObjectId;
}
```

**Приоритет:** ⭐⭐⭐⭐⭐ (Очень высокий)
**Сложность:** Средняя
**Время реализации:** 2-3 недели

---

### 2. 🖼️ Интерактивные анатомические диаграммы

**Текущая ситуация:**
- Есть 3D модели
- Есть статичные изображения в темах
- НЕТ кликабельных 2D схем

**Предлагаемая функциональность:**

#### Интерактивные SVG диаграммы:
- **Режим изучения:**
  - Наведение мыши → всплывающая подсказка с названием
  - Клик → детальная информация (функция, иннервация, кровоснабжение)
  - Подсветка связанных структур

- **Режим тестирования:**
  - "Укажите на диаграмме трапециевидную мышцу"
  - Клик по правильной области → +балл
  - Подсказки при ошибке

- **Многослойные диаграммы:**
  - Переключение слоев (кожа → мышцы → кости)
  - Анимация перехода между слоями

#### Типы диаграмм:
1. **Скелет** - все кости с подписями
2. **Мышечная система** - поверхностные и глубокие мышцы
3. **Нервная система** - основные нервы и сплетения
4. **Сосудистая система** - артерии и вены
5. **Региональные** - шея, спина, конечности

#### Интеграция:
- Добавление диаграмм к темам
- Привязка к квизам (вопросы с диаграммами)
- Экспорт в PDF для печати
- Режим полноэкранного просмотра

**Приоритет:** ⭐⭐⭐⭐ (Высокий)
**Сложность:** Средняя-Высокая
**Время реализации:** 3-4 недели

---

### 3. 🎴 Система флеш-карт (Flashcards)

**Новая функция для эффективного запоминания**

#### Основные возможности:

**Структура карточки:**
```typescript
interface Flashcard {
  _id: ObjectId;
  front: {
    type: 'text' | 'image' | 'diagram';
    content: { ru: string; ro: string };
    image?: string;
  };
  back: {
    content: { ru: string; ro: string };
    additionalInfo?: string;   // Дополнительная информация
  };
  topicId?: ObjectId;          // Связь с темой
  difficulty: 1 | 2 | 3 | 4 | 5;
  tags: string[];
  createdBy: ObjectId;         // Студент или преподаватель
  isPublic: boolean;           // Публичная или приватная
}

interface FlashcardDeck {
  _id: ObjectId;
  name: { ru: string; ro: string };
  description: { ru: string; ro: string };
  cards: ObjectId[];           // Массив flashcard ID
  categoryId?: ObjectId;
  createdBy: ObjectId;
  isPublic: boolean;
  subscribers: ObjectId[];     // Кто подписан на колоду
}

interface StudySession {
  _id: ObjectId;
  userId: ObjectId;
  deckId: ObjectId;
  cardId: ObjectId;
  studiedAt: Date;
  confidence: 1 | 2 | 3 | 4 | 5;  // Насколько хорошо знаю
  nextReview: Date;            // Когда показать снова
}
```

#### Алгоритм интервального повторения (Spaced Repetition):
- **Базовый алгоритм SM-2 (SuperMemo):**
  - Карточка показывается через увеличивающиеся интервалы
  - Интервалы зависят от того, насколько хорошо студент помнит материал

- **Уровни уверенности:**
  1. Не помню совсем (повтор через 1 минуту)
  2. С трудом вспомнил (повтор через 10 минут)
  3. Вспомнил с размышлением (повтор через 1 день)
  4. Легко вспомнил (повтор через 3 дня)
  5. Знаю идеально (повтор через 7+ дней)

#### Режимы изучения:
1. **Новые карточки** - карточки, которые еще не изучались
2. **Повторение** - карточки, которые нужно повторить сегодня
3. **Проблемные** - карточки с низкой уверенностью
4. **Все подряд** - случайный порядок
5. **Экзамен** - режим тестирования без подсказок

#### Функции для студентов:
- Создание своих карточек
- Подписка на публичные колоды преподавателей
- Статистика изучения (сколько карточек в день)
- Прогресс по каждой колоде
- Экспорт/импорт колод (JSON, Anki формат)
- Изучение в веб-приложении и Telegram боте

#### Функции для преподавателей:
- Создание профессиональных колод
- Публикация колод для всех студентов
- Привязка колод к темам курса
- Статистика: сколько студентов используют колоду
- Рекомендации студентам: "Выучи эту колоду перед занятием"

#### Telegram интеграция:
- Команда `/flashcards` - список доступных колод
- Команда `/study <название колоды>` - начать изучение
- Интерактивные кнопки: "Показать ответ" → оценка уверенности
- Ежедневное напоминание: "У вас 15 карточек на повторение"

**Примеры карточек:**
```
Лицевая сторона: "Какая мышца отводит плечо?"
Обратная сторона: "Дельтовидная мышца (m. deltoideus)"

Лицевая сторона: [Изображение мышцы]
Обратная сторона: "Трапециевидная мышца. Функция: приведение лопатки, разгибание шеи"

Лицевая сторона: "Иннервация двуглавой мышцы плеча?"
Обратная сторона: "Мышечно-кожный нерв (C5-C6)"
```

**Приоритет:** ⭐⭐⭐⭐ (Высокий)
**Сложность:** Средняя
**Время реализации:** 2-3 недели

---

### 4. 📊 Аналитика для преподавателей

**Расширенная панель с метриками и отчетами**

#### Dashboard для преподавателя:

**Общая статистика группы:**
- Количество активных студентов (заходили за последние 7 дней)
- Средний прогресс по курсу (% пройденных тем)
- Средний балл по квизам
- Процент сдачи домашних заданий
- Посещаемость занятий

**Детальная аналитика по студентам:**
```typescript
interface StudentAnalytics {
  studentId: ObjectId;
  studentName: string;

  // Активность
  lastActive: Date;
  totalTimeSpent: number;        // В минутах
  loginFrequency: number;        // Заходов в неделю

  // Прогресс
  topicsCompleted: number;
  topicsTotal: number;
  progressPercentage: number;

  // Квизы
  quizzesTaken: number;
  averageQuizScore: number;
  bestScore: number;
  worstScore: number;

  // Домашние задания
  assignmentsSubmitted: number;
  assignmentsTotal: number;
  submissionRate: number;        // %
  averageGrade: number;
  lateSubmissions: number;

  // Проблемные темы
  weakTopics: Array<{
    topicId: ObjectId;
    topicName: string;
    score: number;
  }>;

  // Рекомендации
  suggestions: string[];         // Автогенерируемые рекомендации
}
```

**Визуализация данных:**
1. **Графики прогресса:**
   - Линейный график: прогресс группы по неделям
   - Столбчатая диаграмма: средние баллы по квизам
   - Тепловая карта: активность студентов по дням недели

2. **Сравнительные таблицы:**
   - Рейтинг студентов по баллам
   - Таблица посещаемости
   - Сводка по домашним заданиям

3. **Проблемные зоны:**
   - Какие темы вызывают наибольшие сложности (низкие баллы)
   - Кто из студентов отстает (красная зона)
   - Кто не сдал домашние задания

**Автоматические инсайты:**
- "5 студентов не заходили более 2 недель"
- "Тема 'Мышцы спины' имеет средний балл 65% - возможно, нужно повторить"
- "Студент Иван Иванов улучшил результаты на 25% за последний месяц"
- "Посещаемость вторника ниже на 30% по сравнению с другими днями"

**Экспорт отчетов:**
- PDF: детальный отчет по группе или студенту
- Excel: таблицы для дальнейшего анализа
- CSV: экспорт данных
- Периоды: неделя, месяц, семестр, весь курс

**Уведомления для преподавателя:**
- "Студент X не заходил 7 дней"
- "Новое домашнее задание сдано (осталось 5 непроверенных)"
- "Средний балл по последнему квизу упал на 15%"

**Приоритет:** ⭐⭐⭐⭐⭐ (Очень высокий)
**Сложность:** Средняя
**Время реализации:** 2-3 недели

---

### 5. 💬 Форум/Обсуждения

**Дополнение к существующему чату**

**Чем отличается от чата:**
- Чат: синхронное общение, быстрые вопросы
- Форум: асинхронные обсуждения, детальные ответы, архив знаний

#### Структура форума:

```typescript
interface ForumCategory {
  _id: ObjectId;
  name: { ru: string; ro: string };
  description: { ru: string; ro: string };
  groupId?: ObjectId;           // null = общий форум для всех
  icon: string;
  order: number;
}

interface ForumThread {
  _id: ObjectId;
  categoryId: ObjectId;
  title: { ru: string; ro: string };
  authorId: ObjectId;
  createdAt: Date;

  // Контент
  content: string;              // Markdown
  tags: string[];
  attachments: string[];

  // Метаданные
  views: number;
  isPinned: boolean;            // Закреплен
  isLocked: boolean;            // Закрыт для ответов
  isSolved: boolean;            // Вопрос решен

  // Статистика
  replyCount: number;
  lastReplyAt: Date;
  lastReplyBy: ObjectId;
}

interface ForumPost {
  _id: ObjectId;
  threadId: ObjectId;
  authorId: ObjectId;
  content: string;              // Markdown
  createdAt: Date;
  updatedAt?: Date;

  // Реакции
  upvotes: ObjectId[];          // Кто проголосовал "полезно"
  downvotes: ObjectId[];

  // Метаданные
  isAcceptedAnswer: boolean;    // Лучший ответ (отмечает автор или преподаватель)
  isByTeacher: boolean;
  attachments: string[];
}
```

#### Функциональность:

**Категории форума:**
- Общие вопросы
- Вопросы по темам курса
- Обсуждение домашних заданий
- Помощь с квизами
- Анонсы и новости
- Оффтоп

**Создание темы (thread):**
- Заголовок вопроса
- Детальное описание (Markdown)
- Прикрепление изображений/файлов
- Выбор категории
- Добавление тегов (#остеология #череп)

**Ответы на темы:**
- Написание ответа (Markdown)
- Цитирование других постов
- Прикрепление файлов
- Редактирование своих постов (с отметкой "изменено")

**Система голосования:**
- Upvote (+1) / Downvote (-1) для постов
- Самые полезные ответы поднимаются наверх
- Рейтинг пользователей на основе полезных ответов

**Отметка лучшего ответа:**
- Автор вопроса может отметить ✅ "Это решило мою проблему"
- Преподаватель может отметить лучший ответ
- Тема помечается как "Решено"

**Уведомления:**
- Новый ответ в вашей теме
- Ваш пост получил upvote
- Преподаватель ответил на ваш вопрос
- Упоминание (@username)

**Поиск и фильтры:**
- Поиск по ключевым словам
- Фильтр: только вопросы без ответа
- Фильтр: только решенные вопросы
- Фильтр по тегам
- Сортировка: новые, популярные, без ответов

**Модерация:**
- Преподаватель может закреплять важные темы
- Удаление спама
- Закрытие тем для дальнейших обсуждений
- Перемещение тем между категориями

**Telegram интеграция:**
- Команда `/forum ask <вопрос>` - задать вопрос
- Команда `/forum recent` - последние обсуждения
- Уведомление в Telegram о новых ответах

**Геймификация:**
- Бейдж "Помощник" за 10 полезных ответов
- Бейдж "Эксперт" за 5 лучших ответов
- Рейтинг самых активных участников

**Приоритет:** ⭐⭐⭐⭐ (Высокий)
**Сложность:** Средняя-Высокая
**Время реализации:** 3-4 недели

---

### 6. 🎮 Геймификация

**Система мотивации и вовлечения студентов**

#### Компоненты системы:

**1. Достижения (Achievements/Badges):**

```typescript
interface Achievement {
  _id: ObjectId;
  key: string;                  // Уникальный ключ (e.g., 'first_perfect_quiz')
  name: { ru: string; ro: string };
  description: { ru: string; ro: string };
  icon: string;                 // URL иконки или emoji
  category: 'learning' | 'social' | 'streak' | 'mastery';
  rarity: 'common' | 'rare' | 'epic' | 'legendary';
  points: number;               // Сколько XP дает
  requirements: {
    type: string;
    value: number;
  };
}

interface UserAchievement {
  userId: ObjectId;
  achievementId: ObjectId;
  unlockedAt: Date;
  progress?: number;            // Для прогрессивных достижений
}
```

**Примеры достижений:**

**Категория: Обучение**
- 🎯 "Первый шаг" - Пройдена первая тема
- 📚 "Книжный червь" - Прочитано 10 тем
- 🧠 "Мастер анатомии" - Пройдены все темы курса
- 🎓 "Отличник" - Средний балл по квизам выше 90%
- 💯 "Перфекционист" - Получен 100% в квизе
- 🔥 "Безупречная серия" - 5 квизов подряд с баллом 80%+
- 📖 "Эрудит" - Изучено 50 флеш-карт

**Категория: Социальная активность**
- 💬 "Собеседник" - Отправлено 50 сообщений в чате
- 🤝 "Помощник группы" - 5 полезных ответов на форуме (upvotes)
- ⭐ "Эксперт" - Ответ отмечен как лучший преподавателем
- 👥 "Командный игрок" - Активность в группе 30 дней подряд

**Категория: Упорство**
- 🔥 "Недельная серия" - 7 дней подряд в приложении
- 🚀 "Месячная серия" - 30 дней подряд
- ⚡ "Спринтер" - Пройдено 5 тем за один день
- 🌙 "Сова" - Активность после 23:00

**Категория: Мастерство**
- 💪 "Знаток мышц" - 100% в квизе по миологии
- 🦴 "Костоправ" - 100% в квизе по остеологии
- 🧬 "Невролог" - 100% в квизе по нервной системе
- 🎨 "Коллекционер" - Получено 20 достижений
- 👑 "Легенда" - Все достижения разблокированы

**2. Система уровней (Leveling System):**

```typescript
interface UserLevel {
  userId: ObjectId;
  level: number;                // Текущий уровень (1-100)
  currentXP: number;            // Текущий опыт
  totalXP: number;              // Весь накопленный опыт
  title: string;                // Звание (Новичок, Студент, Эксперт)
}
```

**Таблица уровней:**
- Уровень 1-10: Новичок (Novice)
- Уровень 11-25: Студент (Student)
- Уровень 26-50: Практикант (Practitioner)
- Уровень 51-75: Специалист (Specialist)
- Уровень 76-99: Эксперт (Expert)
- Уровень 100: Мастер анатомии (Anatomy Master)

**Получение опыта (XP):**
- Завершена тема: +50 XP
- Пройден квиз: +20 XP + (балл квиза * 0.5)
- Сдано домашнее задание: +30 XP
- Изучено 10 флеш-карт: +15 XP
- Помощь на форуме (upvote): +10 XP
- Ежедневный вход: +5 XP
- Серия 7 дней: +50 XP (бонус)

**Формула прогрессии:**
```
XP для следующего уровня = 100 * level^1.5
```

**3. Ежедневные задачи (Daily Challenges):**

```typescript
interface DailyChallenge {
  _id: ObjectId;
  date: Date;
  challenges: Array<{
    type: 'complete_topic' | 'take_quiz' | 'study_flashcards' | 'forum_post';
    target: number;             // Сколько нужно сделать
    reward: {
      xp: number;
      bonus?: string;           // Дополнительная награда
    };
  }>;
}

interface UserChallengeProgress {
  userId: ObjectId;
  date: Date;
  completedChallenges: string[];
  claimedRewards: boolean;
}
```

**Примеры ежедневных заданий:**
- 📖 Изучи 1 новую тему (+30 XP)
- ❓ Пройди 1 квиз (+25 XP)
- 🎴 Повтори 20 флеш-карт (+20 XP)
- 💬 Помоги однокурснику на форуме (+15 XP)
- 🔍 Используй функцию поиска (+10 XP)

**4. Таблица лидеров (Leaderboards):**

```typescript
interface Leaderboard {
  period: 'daily' | 'weekly' | 'monthly' | 'all-time';
  groupId?: ObjectId;           // null = глобальная таблица
  rankings: Array<{
    rank: number;
    userId: ObjectId;
    userName: string;
    avatar?: string;
    score: number;
    level: number;
  }>;
}
```

**Типы лидербордов:**
- 🏆 По общему XP
- 📊 По среднему баллу квизов
- 🔥 По текущей серии дней
- 🎯 По количеству завершенных тем
- 💬 По активности на форуме

**Настройки приватности:**
- Показывать меня в глобальной таблице: Да/Нет
- Показывать меня в таблице группы: Да/Нет
- Анонимный режим (показывать как "Студент #1234")

**5. Серии (Streaks):**

```typescript
interface UserStreak {
  userId: ObjectId;
  currentStreak: number;        // Текущая серия дней
  longestStreak: number;        // Рекорд
  lastActiveDate: Date;
  streakStartDate: Date;
}
```

**Механика серии:**
- Засчитывается день, если пользователь выполнил хотя бы одно действие
- Действия: прочитал тему, прошел квиз, изучил карточки
- Прерывается, если пропущен день
- Защита серии: один раз в месяц можно "заморозить" серию на 1 день

**Награды за серии:**
- 7 дней: +50 XP, бейдж "Недельная серия"
- 14 дней: +100 XP
- 30 дней: +300 XP, бейдж "Месячная серия"
- 100 дней: +1000 XP, бейдж "Центурион", эксклюзивная иконка профиля

**6. UI элементы геймификации:**

**Профиль пользователя:**
```
┌─────────────────────────────────────┐
│  👤 Иван Иванов                     │
│  ⭐ Уровень 24: Практикант          │
│                                     │
│  [████████░░] 1,840 / 2,500 XP     │
│                                     │
│  🔥 Серия: 12 дней                  │
│  🏆 Достижения: 15/45               │
│  📊 Средний балл: 87%               │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Последние достижения:         │ │
│  │ 🎯 Перфекционист              │ │
│  │ 📚 Книжный червь              │ │
│  │ 🔥 Недельная серия            │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

**Dashboard с ежедневными заданиями:**
```
Ежедневные задания (20 декабря):
☑ Изучи 1 тему (+30 XP) - Выполнено!
☐ Пройди 1 квиз (+25 XP) - 0/1
☐ Повтори 20 карточек (+20 XP) - 7/20

Прогресс: 1/3 задания | +30 XP сегодня
```

**Уведомления о достижениях:**
```
🎉 Новое достижение!
🎯 "Перфекционист"
Вы получили 100% в квизе!
+50 XP
```

**7. Telegram интеграция:**

- Команда `/stats` - моя статистика (уровень, XP, серия)
- Команда `/achievements` - мои достижения
- Команда `/leaderboard` - таблица лидеров группы
- Команда `/daily` - ежедневные задания
- Уведомление: "🔥 Не забудь продлить серию! Зайди сегодня."
- Уведомление: "🎉 Новое достижение разблокировано!"

**Приоритет:** ⭐⭐⭐⭐ (Высокий)
**Сложность:** Средняя
**Время реализации:** 3-4 недели

---

### 7. 🤖 Улучшения Telegram бота

**Расширение функциональности существующего бота**

#### Новые команды:

```typescript
// Домашние задания
/homework                    // Список активных заданий
/homework <номер>            // Детали задания
/submit                      // Отправить выполненное задание
/mygrade                     // Мои оценки

// Флеш-карты
/flashcards                  // Список доступных колод
/study <название>            // Начать изучение колоды
/mystudystats                // Статистика изучения

// Форум
/forum ask <вопрос>          // Задать вопрос на форуме
/forum recent                // Последние обсуждения
/forum my                    // Мои темы

// Статистика
/stats                       // Моя статистика (уровень, XP, прогресс)
/progress                    // Детальный прогресс по курсу
/leaderboard                 // Таблица лидеров группы

// Достижения
/achievements                // Мои достижения
/daily                       // Ежедневные задания

// Расписание (улучшенное)
/schedule                    // Расписание на неделю
/schedule today              // Занятия сегодня
/schedule next               // Следующее занятие
/notify <время>              // Напомнить о занятии за X минут

// Контент (улучшенное)
/anatomy <запрос>            // Поиск по анатомии (расширенный)
/quiz random                 // Случайный квиз
/quiz <тема>                 // Квиз по теме
/3d                          // Список 3D моделей
/3d <название>               // Ссылка на 3D модель

// Закладки
/bookmarks                   // Мои закладки
/bookmark <URL>              // Добавить в закладки

// Помощь
/help                        // Справка по командам
/settings                    // Настройки уведомлений
```

#### Интерактивные функции:

**1. Флеш-карты в Telegram:**
```
Студент: /study Мышцы руки

Бот: 📚 Колода "Мышцы руки" (25 карточек)
     Новые: 10 | На повторение: 5

[Кнопка: Начать изучение]

---

Бот: Карточка 1/15

     ❓ Какая мышца сгибает предплечье?

[Кнопка: Показать ответ]

Студент нажимает "Показать ответ"

Бот: ✅ Двуглавая мышца плеча (бицепс)

     Иннервация: мышечно-кожный нерв

     Насколько хорошо вы это знали?

[Кнопки: 😫 Не знал | 🤔 С трудом | 😊 Легко | 🎯 Отлично]
```

**2. Интерактивный квиз:**
```
Студент: /quiz Остеология

Бот: 📝 Квиз "Кости черепа" (10 вопросов)

[Кнопка: Начать квиз]

---

Бот: Вопрос 1/10

     Сколько костей в черепе взрослого человека?

[Кнопки:]
[A) 18]  [B) 22]
[C) 26]  [D) 29]

Студент выбирает B) 22

Бот: ✅ Правильно! (+10 баллов)

     22 кости: 8 черепных + 14 лицевых

[Кнопка: Следующий вопрос →]
```

**3. Ежедневные задания:**
```
Студент: /daily

Бот: 📅 Задания на 20 декабря 2024

     ✅ Изучи 1 тему (+30 XP) - Выполнено!
     ☐ Пройди 1 квиз (+25 XP)
     ☐ Повтори 20 карточек (+20 XP)

     Прогресс: 1/3 | +30 XP сегодня

[Кнопка: Пройти квиз] [Кнопка: Изучить карточки]
```

**4. Отправка домашнего задания:**
```
Студент: /submit

Бот: 📝 Выберите задание для сдачи:

[Кнопки:]
[Урок 5: Миология верхней конечности (до 22.12)]
[Урок 6: Нервная система руки (до 25.12)]

Студент выбирает "Урок 5"

Бот: Отправьте ваше выполненное задание:

     - Текстовый ответ
     - Фото
     - Документ (PDF, DOC)
     - Несколько файлов

     Или /cancel для отмены

Студент отправляет фото

Бот: ✅ Задание получено!

     📷 1 фото
     Отправлено: 20.12.2024 14:35

     Ждите проверки преподавателя.

[Кнопка: Добавить комментарий]
```

#### Автоматические уведомления (Smart Notifications):

**Напоминания о занятиях:**
```
🔔 Напоминание!

Через 30 минут начинается занятие:

📚 Урок 7: Мышцы спины
🕐 Время: 15:00 - 16:30
📍 Место: Аудитория 301

[Кнопка: Открыть материалы] [Кнопка: Отменить напоминание]
```

**Новое домашнее задание:**
```
📝 Новое домашнее задание!

Тема: Урок 8 - Артрология
Дедлайн: 25 декабря, 23:59

📎 Прикреплен файл с заданием

[Кнопка: Посмотреть задание] [Кнопка: Отложить напоминание]
```

**Проверка работы:**
```
✅ Ваша работа проверена!

Задание: Урок 5 - Миология
Оценка: 9/10

💬 Комментарий преподавателя:
"Отличная работа! Обратите внимание на иннервацию
трехглавой мышцы."

[Кнопка: Посмотреть детали]
```

**Новый контент:**
```
🎥 Новый видеоурок!

Тема: Биомеханика плечевого сустава
Длительность: 15 минут

Добавлен преподавателем Иваном Петровым

[Кнопка: Открыть видео]
```

**Напоминание о серии:**
```
🔥 Не прерывай серию!

Текущая серия: 11 дней
Рекорд: 14 дней

Зайди сегодня, чтобы продолжить серию!

[Кнопка: Открыть приложение]
```

**Новое достижение:**
```
🎉 Поздравляем!

Получено достижение:
🎯 "Перфекционист"

Вы набрали 100% в квизе!
+50 XP | Уровень: 24 → 25

[Кнопка: Посмотреть все достижения]
```

**Ежедневный дайджест (утром):**
```
☀️ Доброе утро!

📊 Ваша статистика:
- Серия: 12 дней 🔥
- Уровень: 24 (1,840/2,500 XP)
- Место в группе: 3 из 25

📅 Сегодня:
- Занятие в 15:00: Мышцы спины
- 1 непройденный квиз
- 15 карточек на повторение

[Кнопка: Открыть приложение]
```

#### Настройки уведомлений (расширенные):

```
Студент: /settings

Бот: ⚙️ Настройки уведомлений

     Занятия и расписание:
     ☑ Напоминание за 30 минут
     ☑ Напоминание за день

     Домашние задания:
     ☑ Новое задание
     ☑ Напоминание за день до дедлайна
     ☑ Проверка работы

     Квизы и прогресс:
     ☑ Новый квиз доступен
     ☐ Еженедельный отчет о прогрессе

     Социальное:
     ☑ Новый ответ на форуме
     ☑ Упоминание в чате
     ☐ Новый участник группы

     Геймификация:
     ☑ Новое достижение
     ☑ Напоминание о серии
     ☑ Ежедневные задания
     ☐ Таблица лидеров (еженедельно)

     Общее:
     ☑ Утренний дайджест (9:00)
     ☐ Вечерний дайджест (20:00)

     Тихий режим: 22:00 - 8:00

[Кнопки: Изменить | Отключить все | Включить все]
```

#### Inline режим (новое):

**Поиск контента прямо в чате:**
```
В любом чате студент пишет:

@АнатомияБот трапециевидная

Бот показывает inline результаты:
┌─────────────────────────────────────┐
│ 💪 Трапециевидная мышца             │
│ Тема: Мышцы спины                   │
│ [Отправить в чат]                   │
├─────────────────────────────────────┤
│ 🎥 Видео: Трапециевидная мышца      │
│ Длительность: 8 мин                 │
│ [Отправить в чат]                   │
├─────────────────────────────────────┤
│ 🎯 Квиз: Мышцы спины                │
│ 10 вопросов                         │
│ [Отправить в чат]                   │
└─────────────────────────────────────┘

Студент может поделиться контентом с одногруппниками
```

**Приоритет:** ⭐⭐⭐⭐⭐ (Очень высокий)
**Сложность:** Средняя
**Время реализации:** 2-3 недели

---

### 8. 📱 Режим оффлайн (PWA улучшения)

**Усиление существующего Progressive Web App**

#### Текущая ситуация:
- PWA упоминается в коде
- Нет полноценного оффлайн режима

#### Предлагаемая функциональность:

**1. Service Worker с умным кэшированием:**

```typescript
// Стратегии кэширования
const cachingStrategies = {
  // Статические ресурсы: кэш-first
  static: ['/', '/index.html', '/assets/*'],

  // Контент: network-first с fallback на кэш
  content: ['/api/topics/*', '/api/categories/*'],

  // Изображения: кэш с автообновлением
  images: ['/api/media/*'],

  // API: только network (с оффлайн-заглушкой)
  api: ['/api/auth/*', '/api/quiz/submit']
};
```

**2. Оффлайн-доступ к контенту:**

**Скачивание тем для оффлайн:**
```typescript
interface OfflineContent {
  userId: ObjectId;
  downloadedTopics: Array<{
    topicId: ObjectId;
    downloadedAt: Date;
    expiresAt: Date;          // Срок актуальности (7 дней)
    size: number;             // Размер в байтах
  }>;
  downloadedMedia: string[];  // URL скачанных изображений/видео
  totalSize: number;          // Общий размер оффлайн контента
  quota: number;              // Лимит (100 MB для Free, 500 MB для Premium)
}
```

**UI для управления оффлайн контентом:**
```
┌─────────────────────────────────────────┐
│ 📥 Оффлайн контент                      │
├─────────────────────────────────────────┤
│                                         │
│ Использовано: 45 MB / 100 MB           │
│ [███████░░░░░░░░░] 45%                 │
│                                         │
│ ✅ Скачано для оффлайн:                │
│                                         │
│ ☑ Тема: Кости черепа (5 MB)            │
│   Скачано: 19.12.2024                  │
│   [Удалить]                             │
│                                         │
│ ☑ Тема: Мышцы спины (12 MB)            │
│   Скачано: 18.12.2024                  │
│   [Удалить]                             │
│                                         │
│ ☐ Тема: Нервная система руки           │
│   Размер: 8 MB                          │
│   [Скачать для оффлайн]                │
│                                         │
│ [Скачать текущую категорию: 45 MB]     │
│ [Очистить весь оффлайн контент]         │
│                                         │
└─────────────────────────────────────────┘
```

**3. Оффлайн квизы:**

```typescript
interface OfflineQuiz {
  quizId: ObjectId;
  questions: Question[];
  answers: Map<string, string>;  // questionId → selectedAnswer
  isSubmitted: boolean;
  submittedAt?: Date;
}

interface QueuedSubmission {
  type: 'quiz' | 'assignment' | 'progress';
  data: any;
  timestamp: Date;
  retryCount: number;
}
```

**Механика:**
- Студент проходит квиз оффлайн
- Ответы сохраняются локально
- При восстановлении связи автоматически отправляются на сервер
- Индикатор: "⏳ Ожидает отправки (оффлайн)"

**4. Синхронизация прогресса:**

```typescript
// Background Sync API
navigator.serviceWorker.ready.then(registration => {
  return registration.sync.register('sync-progress');
});

// При восстановлении связи:
self.addEventListener('sync', event => {
  if (event.tag === 'sync-progress') {
    event.waitUntil(syncQueuedData());
  }
});
```

**Очередь синхронизации:**
- Просмотренные темы
- Пройденные квизы
- Изученные флеш-карты
- Новые закладки
- Написанные заметки

**5. Индикаторы состояния:**

**В UI приложения:**
```
// Оффлайн режим
🔴 Вы оффлайн. Некоторые функции недоступны.
📥 Доступен оффлайн контент: 3 темы, 2 квиза

// Синхронизация
🔄 Синхронизация... (3 элемента в очереди)

// Онлайн
🟢 Онлайн. Все данные синхронизированы.
```

**На элементах контента:**
```
📚 Тема: Кости черепа
✅ Доступно оффлайн

📚 Тема: Сердечно-сосудистая система
⚠️ Требуется интернет (Скачать: 15 MB)
```

**6. Push-уведомления (даже оффлайн):**

```typescript
// Запрос разрешения
Notification.requestPermission().then(permission => {
  if (permission === 'granted') {
    subscribeUserToPush();
  }
});

// Уведомления работают, даже если приложение закрыто
self.addEventListener('push', event => {
  const data = event.data.json();
  self.registration.showNotification(data.title, {
    body: data.body,
    icon: '/icon-192.png',
    badge: '/badge-72.png',
    data: { url: data.url }
  });
});
```

**Примеры push-уведомлений:**
- "Занятие начинается через 30 минут"
- "Новое домашнее задание"
- "Не забудь продлить серию!"

**7. Умная предзагрузка:**

```typescript
// Автоматически скачивать следующие темы
const prefetchNextTopics = async (currentTopicId: string) => {
  const nextTopics = await getNextTopics(currentTopicId, 2);
  for (const topic of nextTopics) {
    caches.open('content-cache').then(cache => {
      cache.add(`/api/topics/${topic._id}`);
    });
  }
};
```

**8. Квоты хранилища:**

```typescript
// Проверка доступного места
navigator.storage.estimate().then(estimate => {
  const usage = estimate.usage;         // Использовано
  const quota = estimate.quota;         // Доступно
  const percent = (usage / quota) * 100;

  console.log(`Использовано: ${percent}%`);
});

// Запрос постоянного хранилища (не удалится автоматически)
navigator.storage.persist().then(persistent => {
  console.log(`Постоянное хранилище: ${persistent ? 'да' : 'нет'}`);
});
```

**9. Оптимизация данных:**

**Сжатие контента для оффлайн:**
- Изображения: WebP формат, сжатие 70%
- Видео: не скачиваются (только при Premium и явном запросе)
- Текст: gzip компрессия
- 3D модели: упрощенные версии для оффлайн

**Приоритет:** ⭐⭐⭐ (Средний)
**Сложность:** Средняя-Высокая
**Время реализации:** 2-3 недели

---

### 9. 🎥 Живые вебинары/стримы

**Интеграция видеоконференций для онлайн-обучения**

#### Варианты реализации:

**Вариант 1: Интеграция с YouTube Live (проще)**

```typescript
interface LiveStream {
  _id: ObjectId;
  scheduleId: ObjectId;         // Связь с занятием
  groupId: ObjectId;

  // YouTube
  youtubeStreamUrl: string;     // Ссылка на стрим
  youtubeVideoId: string;       // ID видео
  youtubeChatEnabled: boolean;

  // Метаданные
  title: { ru: string; ro: string };
  description: { ru: string; ro: string };
  startTime: Date;
  endTime?: Date;
  status: 'scheduled' | 'live' | 'ended' | 'cancelled';

  // Статистика
  viewerCount: number;
  peakViewers: number;
  recordingUrl?: string;        // Запись после окончания
}
```

**Функциональность:**
- Преподаватель создает YouTube Live стрим
- В админ-панели добавляет ссылку на стрим к занятию
- Студенты видят "🔴 LIVE" на странице занятия
- Встроенный плеер YouTube + чат
- После окончания: автоматическое добавление записи в тему

**Вариант 2: Встроенное решение (Jitsi Meet, Daily.co, Agora)**

```typescript
interface VideoConference {
  _id: ObjectId;
  scheduleId: ObjectId;
  groupId: ObjectId;

  // Конференция
  roomId: string;               // Уникальный ID комнаты
  provider: 'jitsi' | 'daily' | 'agora';
  joinUrl: string;              // Ссылка для студентов
  moderatorUrl: string;         // Ссылка для преподавателя

  // Настройки
  requirePassword: boolean;
  password?: string;
  maxParticipants: number;
  enableRecording: boolean;
  enableChat: boolean;
  enableScreenShare: boolean;
  enableHandRaise: boolean;     // Поднятие руки

  // Метаданные
  startTime: Date;
  endTime?: Date;
  status: 'scheduled' | 'active' | 'ended';

  // Участники
  participants: Array<{
    userId: ObjectId;
    joinedAt: Date;
    leftAt?: Date;
    duration: number;           // Время присутствия в минутах
  }>;

  // Запись
  recordingUrl?: string;
  recordingDuration?: number;
}
```

**Пример с Jitsi Meet (бесплатное, open-source):**

```typescript
// Создание комнаты
const createJitsiRoom = async (schedule: Schedule) => {
  const roomName = `anatomia_${schedule.groupId}_${schedule._id}`;

  const conference = new VideoConference({
    scheduleId: schedule._id,
    groupId: schedule.groupId,
    provider: 'jitsi',
    roomId: roomName,
    joinUrl: `https://meet.jit.si/${roomName}`,
    // ...
  });

  await conference.save();
  return conference;
};

// Встраивание на странице
const JitsiMeeting = ({ conference }) => {
  useEffect(() => {
    const api = new JitsiMeetExternalAPI('meet.jit.si', {
      roomName: conference.roomId,
      parentNode: document.querySelector('#jitsi-container'),
      configOverride: {
        startWithAudioMuted: true,
        startWithVideoMuted: true,
        enableWelcomePage: false,
        prejoinPageEnabled: false,
      },
      interfaceConfigOverride: {
        TOOLBAR_BUTTONS: [
          'microphone', 'camera', 'desktop', 'chat',
          'raisehand', 'fullscreen', 'hangup'
        ],
        SHOW_JITSI_WATERMARK: false,
      },
      userInfo: {
        displayName: currentUser.name,
        email: currentUser.email,
      }
    });

    // Отслеживание участников
    api.addEventListener('participantJoined', (e) => {
      console.log('Joined:', e);
    });

    api.addEventListener('participantLeft', (e) => {
      console.log('Left:', e);
    });

    return () => api.dispose();
  }, []);

  return <div id="jitsi-container" />;
};
```

#### Функциональность для преподавателей:

**Создание вебинара:**
```
┌─────────────────────────────────────────┐
│ 🎥 Создать вебинар                      │
├─────────────────────────────────────────┤
│                                         │
│ Занятие: Урок 7 - Мышцы спины          │
│ Группа: Анатомия 2024-1                 │
│                                         │
│ Дата: 22 декабря 2024                   │
│ Время: 15:00 - 16:30                    │
│                                         │
│ Настройки:                              │
│ ☑ Включить чат                          │
│ ☑ Разрешить демонстрацию экрана         │
│ ☑ Записывать вебинар                    │
│ ☑ Требовать пароль (12345)              │
│ ☐ Зал ожидания (модерация входа)        │
│                                         │
│ Максимум участников: 50                 │
│                                         │
│ [Создать вебинар]                       │
│                                         │
└─────────────────────────────────────────┘
```

**Во время вебинара (панель преподавателя):**
```
┌─────────────────────────────────────────┐
│ 🔴 LIVE: Урок 7 - Мышцы спины          │
│ Участники: 23/50                        │
├─────────────────────────────────────────┤
│                                         │
│ [Видео студентов]                       │
│                                         │
│ Поднятые руки:                          │
│ ✋ Мария Иванова (2 мин назад)          │
│    [Дать слово] [Убрать руку]           │
│                                         │
│ ✋ Петр Сидоров (30 сек назад)          │
│    [Дать слово] [Убрать руку]           │
│                                         │
│ Чат:                                    │
│ Иван: Не слышно звук                    │
│ Ольга: Можно повторить про иннервацию?  │
│                                         │
│ [Демонстрация экрана]                   │
│ [Прекратить запись]                     │
│ [Завершить вебинар]                     │
│                                         │
└─────────────────────────────────────────┘
```

#### Функциональность для студентов:

**До начала вебинара:**
```
📅 Занятие 22 декабря, 15:00

🎥 Вебинар: Мышцы спины
Преподаватель: Иван Петров

⏰ Начало через: 25 минут

[Присоединиться к вебинару]
[Добавить в календарь]
[Получить напоминание за 10 минут]
```

**Во время вебинара:**
```
┌─────────────────────────────────────────┐
│ 🔴 LIVE                                 │
│                                         │
│ [Видео преподавателя]                   │
│                                         │
│ Длительность: 35:42                     │
│ Участников: 23                          │
│                                         │
│ [🎤 Микрофон: выкл]                     │
│ [📹 Камера: выкл]                       │
│ [✋ Поднять руку]                        │
│ [💬 Чат]                                │
│ [📺 Полный экран]                       │
│                                         │
└─────────────────────────────────────────┘
```

**После вебинара:**
```
✅ Вебинар завершен

📊 Ваша статистика:
- Присутствовали: 87 минут из 90
- Задали вопросов: 2
- Активность: Высокая

🎥 Запись доступна:
[Посмотреть запись]
[Скачать запись (245 MB)]

📝 Материалы вебинара:
- Презентация.pdf
- Дополнительные схемы.pdf
```

#### Интеграция с системой:

**Автоматические действия:**
1. За 30 минут до вебинара: уведомление всем студентам
2. За 10 минут: повторное напоминание
3. При начале: уведомление "🔴 Вебинар начался!"
4. Во время: отслеживание присутствия
5. После окончания:
   - Запись автоматически добавляется к теме
   - Статистика посещаемости сохраняется
   - Студенты получают уведомление о доступности записи

**Telegram интеграция:**
- `/webinar next` - следующий вебинар
- `/webinar join` - получить ссылку на текущий вебинар
- Уведомление: "🎥 Вебинар начинается через 10 минут! [Присоединиться]"

#### Аналитика вебинаров:

```typescript
interface WebinarAnalytics {
  conferenceId: ObjectId;

  // Посещаемость
  totalRegistered: number;      // Зарегистрировано
  totalAttended: number;        // Присутствовало
  attendanceRate: number;       // % посещаемости
  averageDuration: number;      // Среднее время присутствия

  // Активность
  questionsAsked: number;
  handRaisesTotal: number;
  chatMessagesTotal: number;

  // Студенты
  students: Array<{
    userId: ObjectId;
    attended: boolean;
    duration: number;
    questionsAsked: number;
    engagement: 'high' | 'medium' | 'low';
  }>;
}
```

**Приоритет:** ⭐⭐⭐⭐ (Высокий)
**Сложность:** Высокая
**Время реализации:** 3-4 недели (Jitsi) или 1-2 недели (YouTube Live)

---

### 10. 🎯 Персонализированные учебные планы

**AI-рекомендации и адаптивное обучение**

#### Концепция:

Система анализирует прогресс и слабые места студента, автоматически рекомендуя, что изучать дальше.

```typescript
interface LearningPath {
  _id: ObjectId;
  userId: ObjectId;
  generatedAt: Date;

  // Анализ слабых мест
  weakTopics: Array<{
    topicId: ObjectId;
    topicName: string;
    score: number;              // Средний балл по квизам
    attempts: number;
    lastAttempt: Date;
    recommendedAction: 'review' | 'practice' | 'restudy';
  }>;

  // Рекомендации
  nextSteps: Array<{
    type: 'topic' | 'quiz' | 'flashcards' | 'video';
    contentId: ObjectId;
    reason: string;             // Почему рекомендуется
    priority: 1 | 2 | 3;
    estimatedTime: number;      // Минуты
  }>;

  // План подготовки к экзамену
  examPreparation?: {
    examDate: Date;
    daysLeft: number;
    readyPercentage: number;
    dailyGoal: string;
    focusAreas: string[];
  };
}
```

#### Алгоритм рекомендаций:

**1. Анализ слабых мест:**
```typescript
const analyzeWeaknesses = async (userId: ObjectId) => {
  const quizResults = await QuizResult.find({ userId });

  const topicScores = new Map();

  for (const result of quizResults) {
    const topicId = result.quizId.topicId;
    const score = result.score / result.totalQuestions * 100;

    if (!topicScores.has(topicId)) {
      topicScores.set(topicId, []);
    }
    topicScores.get(topicId).push(score);
  }

  const weakTopics = [];

  for (const [topicId, scores] of topicScores) {
    const avgScore = scores.reduce((a, b) => a + b) / scores.length;

    if (avgScore < 70) {
      weakTopics.push({
        topicId,
        avgScore,
        attempts: scores.length,
        recommendedAction: avgScore < 50 ? 'restudy' : 'practice'
      });
    }
  }

  return weakTopics.sort((a, b) => a.avgScore - b.avgScore);
};
```

**2. Генерация рекомендаций:**
```typescript
const generateRecommendations = async (userId: ObjectId) => {
  const weakTopics = await analyzeWeaknesses(userId);
  const completedTopics = await getCompletedTopics(userId);
  const currentStreak = await getUserStreak(userId);

  const recommendations = [];

  // Повторить слабые темы
  if (weakTopics.length > 0) {
    recommendations.push({
      type: 'topic',
      contentId: weakTopics[0].topicId,
      reason: `Средний балл: ${weakTopics[0].avgScore}%. Рекомендуем повторить.`,
      priority: 1,
      estimatedTime: 30
    });
  }

  // Попрактиковаться с флеш-картами
  if (weakTopics.length > 0) {
    const deck = await getFlashcardDeck(weakTopics[0].topicId);
    if (deck) {
      recommendations.push({
        type: 'flashcards',
        contentId: deck._id,
        reason: 'Закрепите знания с помощью флеш-карт',
        priority: 2,
        estimatedTime: 15
      });
    }
  }

  // Продолжить обучение
  const nextTopic = await getNextUncompletedTopic(userId);
  if (nextTopic) {
    recommendations.push({
      type: 'topic',
      contentId: nextTopic._id,
      reason: 'Продолжите изучение курса',
      priority: 3,
      estimatedTime: 25
    });
  }

  return recommendations;
};
```

#### UI персонализированного плана:

```
┌─────────────────────────────────────────┐
│ 🎯 Ваш учебный план                     │
├─────────────────────────────────────────┤
│                                         │
│ 📊 Анализ вашего прогресса:             │
│                                         │
│ Сильные стороны:                        │
│ ✅ Остеология: 92%                      │
│ ✅ Артрология: 88%                      │
│                                         │
│ Требуют внимания:                       │
│ ⚠️ Миология: 65%                        │
│ ⚠️ Нервная система: 58%                 │
│                                         │
│ ─────────────────────────────────────   │
│                                         │
│ 💡 Рекомендации на сегодня:             │
│                                         │
│ 1. Приоритет: Высокий                   │
│    📚 Повторить: "Мышцы спины"          │
│    Причина: Средний балл 58%            │
│    Время: ~30 минут                     │
│    [Начать изучение]                    │
│                                         │
│ 2. Приоритет: Средний                   │
│    🎴 Флеш-карты: "Нервы руки"          │
│    Причина: Закрепить материал          │
│    Время: ~15 минут                     │
│    [Начать повторение]                  │
│                                         │
│ 3. Приоритет: Низкий                    │
│    ✅ Квиз: "Кости нижней конечности"   │
│    Причина: Проверить знания            │
│    Время: ~10 минут                     │
│    [Пройти квиз]                        │
│                                         │
│ ─────────────────────────────────────   │
│                                         │
│ 📅 Подготовка к экзамену:               │
│                                         │
│ Дата экзамена: 15 января 2025           │
│ Осталось: 26 дней                       │
│                                         │
│ Готовность: 68%                         │
│ [███████░░░░░░░░░░]                     │
│                                         │
│ Ваша цель: 30 минут/день                │
│ Сегодня: 0 минут                        │
│                                         │
│ Фокус на этой неделе:                   │
│ • Миология (мышцы туловища и спины)     │
│ • Нервная система (сплетения)           │
│                                         │
│ [Обновить план]                         │
│                                         │
└─────────────────────────────────────────┘
```

#### Адаптивная сложность квизов:

```typescript
const getAdaptiveQuiz = async (userId: ObjectId, topicId: ObjectId) => {
  const userLevel = await getUserSkillLevel(userId, topicId);

  let difficulty;
  if (userLevel < 50) {
    difficulty = 'easy';        // Легкие вопросы
  } else if (userLevel < 80) {
    difficulty = 'medium';      // Средние вопросы
  } else {
    difficulty = 'hard';        // Сложные вопросы
  }

  const questions = await QuizQuestion.find({
    topicId,
    difficulty
  }).limit(10);

  return questions;
};
```

**Механика:**
- Новичок → легкие вопросы
- Средний уровень → вопросы посложнее
- Эксперт → сложные клинические вопросы

#### Автоматические напоминания:

**Индивидуальные напоминания в Telegram:**
```
📚 Время повторить!

Вы проходили тему "Мышцы спины" 7 дней назад.
Средний балл: 65%

Рекомендуем повторить сегодня для лучшего запоминания.

[Повторить тему] [Напомнить позже]
```

**Еженедельный отчет:**
```
📊 Ваш прогресс за неделю

Изучено тем: 3
Пройдено квизов: 5
Средний балл: 78% (↑ 5%)

🎯 Цель на следующую неделю:
Повторить "Нервная система" и поднять балл до 75%

[Посмотреть детальный отчет]
```

**Приоритет:** ⭐⭐⭐ (Средний)
**Сложность:** Высокая
**Время реализации:** 3-4 недели

---

## 📚 ДОПОЛНИТЕЛЬНЫЙ КОНТЕНТ

### 11. Новые разделы анатомии

**Расширение существующих категорий:**

#### 1. Клиническая анатомия

```typescript
interface ClinicalCase {
  _id: ObjectId;
  title: { ru: string; ro: string };
  category: string;             // Остеология, Миология, etc.

  // Клинический случай
  patientInfo: {
    age: number;
    gender: 'male' | 'female';
    symptoms: { ru: string[]; ro: string[] };
  };

  caseDescription: { ru: string; ro: string };

  // Анатомическая связь
  relatedStructures: ObjectId[];  // Связанные анатомические структуры
  anatomicalExplanation: { ru: string; ro: string };

  // Диагностика
  diagnosticSteps: Array<{
    step: { ru: string; ro: string };
    reasoning: { ru: string; ro: string };
  }>;

  // Образовательная ценность
  learningObjectives: { ru: string[]; ro: string[] };
  keyTakeaways: { ru: string[]; ro: string[] };

  // Мультимедиа
  images: string[];             // Рентген, МРТ, схемы
  videos: string[];

  difficulty: 1 | 2 | 3 | 4 | 5;
  tier: 'free' | 'basic' | 'premium';
}
```

**Примеры клинических случаев:**
- "Перелом ключицы: анатомия и последствия"
- "Синдром запястного канала: анатомические предпосылки"
- "Грыжа межпозвоночного диска: как это происходит"
- "Травма вращательной манжеты плеча"

#### 2. Пальпация (для массажистов)

```typescript
interface PalpationGuide {
  _id: ObjectId;
  structure: ObjectId;          // Ссылка на мышцу/кость
  name: { ru: string; ro: string };

  // Инструкции по пальпации
  position: { ru: string; ro: string };  // Положение пациента
  landmarks: { ru: string[]; ro: string[] };  // Ориентиры
  technique: { ru: string; ro: string };

  // Что должно ощущаться
  normalFeel: { ru: string; ro: string };
  abnormalSigns: { ru: string[]; ro: string[] };

  // Мультимедиа
  images: string[];
  videoDemo: string;            // Видео демонстрация

  // Сложность
  difficulty: 'beginner' | 'intermediate' | 'advanced';
  commonMistakes: { ru: string[]; ro: string[] };
}
```

**Примеры:**
- Пальпация трапециевидной мышцы
- Пальпация остистых отростков позвонков
- Пальпация триггерных точек
- Пальпация лимфатических узлов

#### 3. Функциональная анатомия / Биомеханика

```typescript
interface MovementAnalysis {
  _id: ObjectId;
  movementName: { ru: string; ro: string };
  joint: string;                // Сустав

  // Биомеханика
  planeOfMotion: 'sagittal' | 'frontal' | 'transverse';
  rangeOfMotion: {
    normal: number;             // Градусы
    min: number;
    max: number;
  };

  // Участвующие мышцы
  primeMovers: Array<{
    muscleId: ObjectId;
    role: 'agonist' | 'synergist' | 'antagonist' | 'stabilizer';
  }>;

  // Анализ движения
  phases: Array<{
    name: { ru: string; ro: string };
    description: { ru: string; ro: string };
    activeMuscles: ObjectId[];
  }>;

  // Клиническая значимость
  commonInjuries: { ru: string[]; ro: string[] };
  compensatoryPatterns: { ru: string; ro: string };

  // Мультимедиа
  animation: string;            // 3D анимация движения
  images: string[];
}
```

**Примеры:**
- Сгибание в плечевом суставе: биомеханика и мышцы
- Ходьба: фазы и работа мышц
- Приседание: анализ движения
- Подъем руки над головой

#### 4. Патологическая анатомия (базовая)

**Изменения при заболеваниях:**
- Артроз: изменения в суставе
- Сколиоз: искривление позвоночника
- Остеопороз: изменения костной ткани
- Миозит: воспаление мышц

#### 5. Анатомия в движении

**Интеграция с видео:**
- Медленные видео движений с выделением работающих мышц
- Анимации сокращения мышц
- 3D модели в движении

**Приоритет:** ⭐⭐⭐ (Средний)
**Сложность:** Средняя (требуется создание контента)
**Время реализации:** Зависит от объема контента

---

### 12. Интерактивные упражнения

**Новые типы вопросов для квизов:**

#### 1. Drag & Drop (Перетаскивание)

```typescript
interface DragDropQuestion {
  type: 'drag-drop';
  question: { ru: string; ro: string };

  // Изображение с зонами
  backgroundImage: string;
  dropZones: Array<{
    id: string;
    x: number;                  // Координаты
    y: number;
    width: number;
    height: number;
    correctAnswer: string;
  }>;

  // Элементы для перетаскивания
  draggableItems: Array<{
    id: string;
    label: { ru: string; ro: string };
  }>;
}
```

**Пример:**
```
Вопрос: Перетащите названия мышц на правильные области

[Изображение спины с контурами мышц]

Перетаскиваемые элементы:
- Трапециевидная
- Широчайшая мышца спины
- Ромбовидная
- Дельтовидная
```

#### 2. Сопоставление (Matching)

```typescript
interface MatchingQuestion {
  type: 'matching';
  question: { ru: string; ro: string };

  leftColumn: Array<{
    id: string;
    content: { ru: string; ro: string };
  }>;

  rightColumn: Array<{
    id: string;
    content: { ru: string; ro: string };
  }>;

  correctPairs: Map<string, string>;  // leftId → rightId
}
```

**Пример:**
```
Соедините мышцу с её функцией:

Двуглавая мышца плеча    ⟷   Разгибание предплечья
Трёхглавая мышца плеча   ⟷   Сгибание предплечья
Дельтовидная мышца       ⟷   Отведение плеча
Большая грудная мышца    ⟷   Приведение плеча
```

#### 3. Заполнение пропусков (Fill in the Blanks)

```typescript
interface FillBlankQuestion {
  type: 'fill-blank';
  question: { ru: string; ro: string };

  // Текст с пропусками: "Трапециевидная мышца иннервируется _____ нервом"
  template: { ru: string; ro: string };

  blanks: Array<{
    index: number;
    correctAnswer: string;
    caseSensitive: boolean;
    acceptableVariants: string[];  // Синонимы
  }>;
}
```

**Пример:**
```
Заполните пропуски:

Двуглавая мышца плеча иннервируется _______ нервом
и выполняет функцию _______ предплечья.

Правильные ответы:
1. мышечно-кожным
2. сгибания
```

#### 4. Аудио-квизы (Произношение терминов)

```typescript
interface AudioQuestion {
  type: 'audio-pronunciation';
  audioUrl: string;             // Аудио с произношением термина

  options: Array<{
    text: { ru: string; ro: string };
    isCorrect: boolean;
  }>;
}
```

**Пример:**
```
🔊 Прослушайте произношение термина

[Аудио: "Стерноклейдомастоидеус"]

Какая это мышца?
A) Грудино-ключично-сосцевидная
B) Трапециевидная
C) Дельтовидная
D) Лестничная
```

#### 5. Интерактивные диаграммы (как вопрос квиза)

```typescript
interface DiagramClickQuestion {
  type: 'diagram-click';
  question: { ru: string; ro: string };
  imageUrl: string;

  clickableAreas: Array<{
    id: string;
    polygon: number[][];        // Координаты области
    label: string;
    isCorrect: boolean;
  }>;
}
```

**Пример:**
```
Укажите на диаграмме трапециевидную мышцу

[Интерактивное изображение мышц спины]

Студент кликает на область → получает обратную связь
```

**Приоритет:** ⭐⭐⭐⭐ (Высокий)
**Сложность:** Средняя-Высокая
**Время реализации:** 2-3 недели

---

### 13. Дополнительный мультимедийный контент

#### 1. Анимации процессов

**Что анимировать:**
- Сокращение мышцы (механизм скольжения нитей)
- Передача нервного импульса
- Работа сустава при движении
- Кровоток через сердце
- Дыхательные движения

**Формат:**
- GIF анимации
- WebM видео (легковесные)
- Lottie анимации (векторные, интерактивные)

#### 2. Подкасты по анатомии

```typescript
interface Podcast {
  _id: ObjectId;
  title: { ru: string; ro: string };
  description: { ru: string; ro: string };

  audioUrl: string;
  duration: number;             // Секунды

  // Метаданные
  host: string;                 // Ведущий
  guests: string[];             // Гости
  recordedAt: Date;

  // Таймкоды
  chapters: Array<{
    time: number;               // Секунды от начала
    title: { ru: string; ro: string };
  }>;

  // Связь с контентом
  relatedTopics: ObjectId[];

  transcript?: { ru: string; ro: string };  // Текстовая расшифровка
}
```

**Темы подкастов:**
- Интересные факты об анатомии
- Интервью с преподавателями
- Истории из практики массажистов
- Разбор сложных тем

#### 3. VR-туры (будущее)

**Концепция:**
- 360° панорамные изображения анатомических структур
- Виртуальная экскурсия внутри тела
- VR для Oculus/Meta Quest

**Приоритет:** ⭐⭐ (Низкий, долгосрочно)
**Сложность:** Очень высокая
**Время реализации:** Месяцы

---

## 🔧 ТЕХНИЧЕСКИЕ УЛУЧШЕНИЯ

### 14. API документация (Swagger/OpenAPI)

**Автоматическая генерация документации API**

```typescript
// server/src/swagger.ts
import swaggerJsdoc from 'swagger-jsdoc';
import swaggerUi from 'swagger-ui-express';

const options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'Anatomia API',
      version: '2.0.0',
      description: 'API для платформы интерактивного обучения анатомии',
    },
    servers: [
      { url: 'http://localhost:3000', description: 'Development' },
      { url: 'https://api.anatomia.com', description: 'Production' }
    ],
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT',
        }
      }
    }
  },
  apis: ['./src/routes/*.ts', './src/models/*.ts'],
};

const specs = swaggerJsdoc(options);

// В app.ts
app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(specs));
```

**Аннотации в коде:**
```typescript
/**
 * @swagger
 * /api/topics:
 *   get:
 *     summary: Получить список тем
 *     tags: [Topics]
 *     parameters:
 *       - in: query
 *         name: categoryId
 *         schema:
 *           type: string
 *         description: ID категории
 *     responses:
 *       200:
 *         description: Список тем
 *         content:
 *           application/json:
 *             schema:
 *               type: array
 *               items:
 *                 $ref: '#/components/schemas/Topic'
 */
router.get('/topics', getTopics);
```

**Доступ:**
- http://localhost:3000/api-docs
- Интерактивная документация с возможностью тестирования запросов

**Приоритет:** ⭐⭐⭐ (Средний)
**Сложность:** Низкая
**Время реализации:** 1 неделя

---

### 15. Мобильное приложение (React Native)

**Native iOS/Android приложение**

**Преимущества перед веб-версией:**
- Push-уведомления работают надежнее
- Лучшая производительность
- Оффлайн режим удобнее
- Доступ к камере (для AR в будущем)
- Публикация в App Store / Google Play

**Технологии:**
- React Native + TypeScript
- Expo (для быстрого старта)
- Переиспользование API backend
- Общий UI kit с веб-версией (где возможно)

**Приоритет:** ⭐⭐⭐ (Средний, долгосрочно)
**Сложность:** Высокая
**Время реализации:** 2-3 месяца

---

### 16. Accessibility (Доступность)

**WCAG 2.1 Level AA соответствие:**

1. **Навигация с клавиатуры:**
   - Tab/Shift+Tab между элементами
   - Enter/Space для активации
   - Escape для закрытия модалов

2. **Screen reader поддержка:**
   - Aria-labels для всех интерактивных элементов
   - Семантический HTML
   - Альтернативный текст для изображений

3. **Контрастность:**
   - Минимум 4.5:1 для обычного текста
   - Минимум 3:1 для крупного текста

4. **Масштабирование:**
   - Текст увеличивается до 200% без потери функциональности
   - Responsive дизайн

**Приоритет:** ⭐⭐⭐ (Средний)
**Сложность:** Средняя
**Время реализации:** 2-3 недели

---

### 17. Версионирование контента

**История изменений тем/контента:**

```typescript
interface ContentVersion {
  _id: ObjectId;
  contentType: 'topic' | 'category' | 'quiz' | 'protocol';
  contentId: ObjectId;
  version: number;

  // Данные версии
  data: any;                    // Полная копия контента на тот момент

  // Метаданные
  createdBy: ObjectId;
  createdAt: Date;
  changeDescription: string;    // Что изменилось

  // Сравнение
  diff?: string;                // JSON Patch формат
}
```

**Функциональность:**
- Откат к предыдущей версии
- Сравнение версий (diff)
- История изменений
- Восстановление удаленного контента

**Приоритет:** ⭐⭐ (Низкий)
**Сложность:** Средняя
**Время реализации:** 1-2 недели

---

### 18. Расширенный поиск

**Улучшение существующего поиска:**

```typescript
interface SearchFilters {
  query: string;

  // Фильтры
  contentTypes: Array<'topic' | 'protocol' | 'triggerpoint' | '3dmodel'>;
  categories: ObjectId[];
  difficulty: number[];         // 1-5
  tier: Array<'free' | 'basic' | 'premium'>;

  // Сортировка
  sortBy: 'relevance' | 'date' | 'popularity' | 'difficulty';
  sortOrder: 'asc' | 'desc';

  // Пагинация
  page: number;
  limit: number;
}

interface SearchResult {
  type: string;
  id: ObjectId;
  title: string;
  description: string;
  thumbnailUrl?: string;
  relevanceScore: number;
  highlightedText: string;      // Фрагмент с подсвеченным совпадением
}
```

**UI фильтров:**
```
┌─────────────────────────────────────────┐
│ 🔍 Поиск: [мышцы руки___________] 🔎   │
├─────────────────────────────────────────┤
│                                         │
│ Фильтры:                                │
│                                         │
│ Тип контента:                           │
│ ☑ Темы  ☑ Протоколы  ☐ Триггерные точки│
│ ☐ 3D модели  ☐ Квизы                    │
│                                         │
│ Категория:                              │
│ ☑ Миология  ☐ Остеология  ☐ Артрология │
│                                         │
│ Сложность:                              │
│ ☐ 1  ☑ 2  ☑ 3  ☐ 4  ☐ 5                │
│                                         │
│ Доступ:                                 │
│ ☑ Бесплатно  ☑ Basic  ☑ Premium         │
│                                         │
│ Сортировка: [По релевантности ▼]        │
│                                         │
│ [Применить фильтры] [Сбросить]          │
│                                         │
└─────────────────────────────────────────┘

Результаты (23):

1. 📚 Мышцы верхней конечности
   Миология • Сложность: 3 • Premium
   ...анатомия **мышц руки**, включая сгибатели...

2. 🎥 Видео: Мышцы предплечья
   Видео • 15 минут • Basic
   ...детальный разбор **мышц руки** от запястья...
```

**Приоритет:** ⭐⭐⭐ (Средний)
**Сложность:** Средняя
**Время реализации:** 1-2 недели

---

### 19. Экспорт данных студента

**PDF экспорт:**

```typescript
interface ExportOptions {
  userId: ObjectId;

  includeProgress: boolean;
  includeNotes: boolean;
  includeBookmarks: boolean;
  includeCertificates: boolean;
  includeQuizResults: boolean;

  format: 'pdf' | 'excel' | 'json';
  dateRange?: {
    from: Date;
    to: Date;
  };
}

// Генерация PDF с использованием библиотеки (pdfkit, puppeteer)
const generateProgressReport = async (userId: ObjectId) => {
  const user = await User.findById(userId);
  const progress = await Progress.find({ userId });
  const certificates = await Certificate.find({ userId });

  // Генерация PDF
  const pdf = new PDFDocument();

  pdf.fontSize(20).text('Отчет о прогрессе обучения');
  pdf.fontSize(12).text(`Студент: ${user.name}`);
  pdf.text(`Дата: ${new Date().toLocaleDateString()}`);

  // Статистика
  pdf.text(`Пройдено тем: ${progress.topicsCompleted.length}`);
  pdf.text(`Средний балл: ${progress.averageQuizScore}%`);

  // Сертификаты
  pdf.addPage();
  pdf.fontSize(16).text('Сертификаты');
  for (const cert of certificates) {
    pdf.text(`- ${cert.type}: ${cert.issuedAt.toLocaleDateString()}`);
  }

  return pdf;
};
```

**Что можно экспортировать:**
- Полный отчет о прогрессе (PDF)
- Все заметки (PDF/TXT)
- Закладки с ссылками (HTML/PDF)
- Результаты квизов (Excel)
- Сертификаты (PDF)
- Все данные профиля (JSON - GDPR compliance)

**Приоритет:** ⭐⭐ (Низкий)
**Сложность:** Средняя
**Время реализации:** 1-2 недели

---

### 20. Белая маркировка (White-label)

**Возможность ребрендинга для других школ:**

```typescript
interface TenantConfig {
  _id: ObjectId;
  subdomain: string;            // school1.anatomia.com

  // Брендинг
  branding: {
    schoolName: { ru: string; ro: string };
    logo: string;
    primaryColor: string;
    secondaryColor: string;
    favicon: string;
  };

  // Контент
  ownContent: ObjectId[];       // Собственный контент школы
  sharedContent: ObjectId[];    // Общий контент платформы

  // Настройки
  enabledFeatures: string[];    // Какие модули включены
  customDomain?: string;        // school.com

  // Платежи
  paymentConfig: {
    provider: 'paypal' | 'stripe';
    accountId: string;
  };
}
```

**Мульти-тенантность:**
- Каждая школа имеет свой поддомен
- Собственный брендинг (логотип, цвета)
- Выборочные модули (чат, вебинары, etc.)
- Отдельная база студентов и групп
- Общий контент + собственный контент

**Приоритет:** ⭐ (Низкий, бизнес-решение)
**Сложность:** Очень высокая
**Время реализации:** 2-3 месяца

---

## 📋 ПРИОРИТЕЗАЦИЯ

### 🔥 Реализовать в первую очередь (Высокий ROI):

1. **Система домашних заданий** - критично для учебного процесса
2. **Аналитика для преподавателей** - важная функция для управления группами
3. **Улучшения Telegram бота** - расширение популярной функции
4. **Флеш-карты** - эффективный инструмент запоминания
5. **Интерактивные упражнения** - разнообразие в обучении

### ⚡ Средний приоритет:

6. **Интерактивные диаграммы** - улучшение визуального обучения
7. **Форум/обсуждения** - дополнение к коммуникации
8. **Геймификация** - повышение мотивации
9. **Живые вебинары** - синхронное обучение
10. **PWA оффлайн режим** - удобство использования

### 🔮 Долгосрочные проекты:

11. **Персонализированные учебные планы** - AI-функциональность
12. **Новые разделы контента** - требует создания материалов
13. **Мобильное приложение** - большой проект
14. **Accessibility улучшения** - постепенное внедрение
15. **Белая маркировка** - бизнес-расширение

---

## 💰 Оценка трудозатрат

### Быстрые победы (1-2 недели):
- API документация
- Расширенный поиск с фильтрами
- Экспорт данных
- Версионирование контента

### Средние проекты (2-4 недели):
- Система домашних заданий
- Аналитика для преподавателей
- Флеш-карты с интервальным повторением
- Улучшения Telegram бота
- Интерактивные упражнения
- PWA оффлайн режим

### Крупные проекты (1-3 месяца):
- Интерактивные анатомические диаграммы
- Форум/обсуждения
- Геймификация (полная система)
- Живые вебинары
- Персонализированные планы (AI)
- Мобильное приложение
- Белая маркировка

---

## 🎯 Рекомендуемая дорожная карта

### Фаза 1 (Месяц 1-2): Образовательные инструменты
1. Система домашних заданий
2. Флеш-карты
3. Интерактивные упражнения (drag-drop, matching)

### Фаза 2 (Месяц 3-4): Аналитика и коммуникация
4. Аналитика для преподавателей
5. Форум/обсуждения
6. Расширение Telegram бота

### Фаза 3 (Месяц 5-6): Вовлечение
7. Геймификация (базовая)
8. Интерактивные диаграммы
9. PWA оффлайн режим

### Фаза 4 (Месяц 7-8): Продвинутые функции
10. Живые вебинары
11. Персонализированные планы
12. Новый контент (клиническая анатомия, пальпация)

### Фаза 5 (Месяц 9+): Масштабирование
13. Мобильное приложение
14. Accessibility полное соответствие
15. Белая маркировка (если нужно)

---

## 📞 Следующие шаги

**Что вам больше всего интересно реализовать в первую очередь?**

Я могу помочь с:
1. Детальным техническим планированием любой из функций
2. Написанием кода для реализации
3. Созданием дизайна UI/UX
4. Настройкой инфраструктуры
5. Миграцией данных при необходимости

Просто скажите, с чего хотите начать, и я приступлю к работе!

---

**Дата создания:** 20 декабря 2024
**Версия:** 1.0
