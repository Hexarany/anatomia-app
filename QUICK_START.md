# Быстрый старт / Start Rapid

## Требования / Cerințe

- Node.js 18+
- MongoDB 6+
- npm или yarn

## Установка / Instalare

### 1. Установите зависимости / Instalați dependențele

```bash
# В корневой директории / În directorul rădăcină
npm install

# Или установите отдельно / Sau instalați separat
npm install --prefix client
npm install --prefix server
```

### 2. Настройте MongoDB / Configurați MongoDB

Убедитесь, что MongoDB запущен на вашем компьютере:
```bash
# Windows
mongod

# Linux/Mac
sudo systemctl start mongodb
```

Или используйте MongoDB Atlas (облачная БД):
1. Создайте аккаунт на https://www.mongodb.com/cloud/atlas
2. Создайте кластер
3. Получите connection string

### 3. Настройте переменные окружения / Configurați variabilele de mediu

```bash
# Скопируйте пример / Copiați exemplul
cd server
cp .env.example .env
```

Отредактируйте `server/.env`:
```env
PORT=3000
MONGODB_URI=mongodb://localhost:27017/anatomia
JWT_SECRET=your-secret-key-change-this
CORS_ORIGIN=http://localhost:5173
```

### 4. Наполните базу данных / Populați baza de date

```bash
cd server
npm run seed
```

Это создаст примеры категорий, тем и тестов.

### 5. Запустите приложение / Lansați aplicația

#### Вариант 1: Запуск Frontend и Backend отдельно

**Terminal 1 - Backend:**
```bash
cd server
npm run dev
```

**Terminal 2 - Frontend:**
```bash
cd client
npm run dev
```

#### Вариант 2: Запуск всего одной командой

```bash
# В корневой директории
npm run dev
```

### 6. Откройте приложение / Deschideți aplicația

- Frontend: http://localhost:5173
- Backend API: http://localhost:3000/api

## Структура проекта / Structura Proiectului

```
Anatomia/
├── client/                 # React Frontend
│   ├── src/
│   │   ├── components/    # Компоненты React
│   │   ├── pages/         # Страницы
│   │   ├── i18n/          # Переводы RU/RO
│   │   ├── types/         # TypeScript типы
│   │   └── theme.ts       # Тема оформления
│   └── package.json
│
├── server/                # Node.js Backend
│   ├── src/
│   │   ├── models/       # MongoDB модели
│   │   ├── routes/       # API маршруты
│   │   ├── controllers/  # Контроллеры
│   │   └── scripts/      # Утилиты (seed и т.д.)
│   └── package.json
│
└── README.md
```

## Основные команды / Comenzi Principale

### Development

```bash
# Запуск в режиме разработки
npm run dev

# Только frontend
npm run dev:client

# Только backend
npm run dev:server
```

### Production

```bash
# Собрать все
npm run build

# Запустить production сервер
npm start
```

### Database

```bash
# Наполнить базу данных примерами
cd server
npm run seed
```

## Первые шаги / Primii Pași

### 1. Изучите главную страницу
Откройте http://localhost:5173 и ознакомьтесь с интерфейсом

### 2. Просмотрите категории
Нажмите на любую категорию (например, "Остеология")

### 3. Откройте тему
Выберите тему (например, "Череп")

### 4. Попробуйте интерактивные элементы
- Переключите вкладки (Описание / 3D Модель / Изображения)
- Попробуйте вращать 3D модель мышью
- Пройдите тест

### 5. Переключите язык
В правом верхнем углу переключите между Русским и Румынским

## Добавление контента / Adăugarea Conținutului

### Через API

#### Создать категорию:
```bash
curl -X POST http://localhost:3000/api/categories \
  -H "Content-Type: application/json" \
  -d '{
    "name": {"ru": "Новая категория", "ro": "Categorie nouă"},
    "description": {"ru": "Описание", "ro": "Descriere"},
    "slug": "new-category",
    "order": 6
  }'
```

#### Создать тему:
```bash
curl -X POST http://localhost:3000/api/topics \
  -H "Content-Type: application/json" \
  -d '{
    "categoryId": "CATEGORY_ID_HERE",
    "name": {"ru": "Новая тема", "ro": "Subiect nou"},
    "description": {"ru": "Описание", "ro": "Descriere"},
    "content": {"ru": "Содержание...", "ro": "Conținut..."},
    "slug": "new-topic",
    "order": 1
  }'
```

### Загрузка изображений:
```bash
curl -X POST http://localhost:3000/api/media/upload \
  -F "file=@/path/to/image.jpg"
```

## Решение проблем / Depanare

### MongoDB не запускается

**Windows:**
```bash
# Запустите MongoDB как службу
net start MongoDB
```

**Linux/Mac:**
```bash
sudo systemctl start mongodb
# или
brew services start mongodb-community
```

### Ошибка порта занят (Port already in use)

```bash
# Найдите процесс, использующий порт
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Linux/Mac
lsof -ti:3000
kill -9 <PID>
```

### Не устанавливаются зависимости

```bash
# Очистите кэш npm
npm cache clean --force

# Удалите node_modules и установите заново
rm -rf node_modules package-lock.json
npm install
```

## Следующие шаги / Pașii Următori

1. **Добавьте собственный контент**
   - Создайте новые категории и темы
   - Загрузите изображения и видео
   - Добавьте 3D модели

2. **Настройте дизайн**
   - Измените цвета в `client/src/theme.ts`
   - Добавьте логотип

3. **Разверните в production**
   - Настройте MongoDB Atlas
   - Разверните на Vercel/Netlify (frontend)
   - Разверните на Railway/Render (backend)

4. **Изучите документацию**
   - Прочитайте `DEVELOPER_GUIDE.md` для деталей разработки
   - Ознакомьтесь с API endpoints

## Поддержка / Suport

При возникновении проблем:
1. Проверьте логи в консоли браузера (F12)
2. Проверьте логи сервера в терминале
3. Убедитесь, что MongoDB запущен
4. Проверьте, что все зависимости установлены

---

**Успешной разработки! / Dezvoltare de succes!** 🚀
